[
  {
    "value": "Alert with level {log.level} status {wiz.issues.status} type {wiz.issues.type} for rule {rule.name}",
    "conditions": [
      {
        "field": "log.level"
      },
      {
        "field": "wiz.issues.status"
      },
      {
        "field": "wiz.issues.type"
      },

      {
        "field": "rule.name"
      }
    ]
  },
  {
    "value": "Alert for rule {rule.name}",
    "conditions": [
      {
        "field": "rule.name"
      }
    ]
  },
  {
    "value": "Alert for rule id {rule.id}",
    "conditions": [
      {
        "field": "rule.id"
      }
    ]
  }
]
