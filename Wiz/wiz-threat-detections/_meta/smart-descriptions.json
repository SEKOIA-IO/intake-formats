[
  {
    "value": "Threat detection on {cloud.account.name}({cloud.account.id}): {event.reason}",
    "conditions": [
      { "field": "cloud.account.name" },
      { "field": "cloud.account.id" },
      { "field": "event.reason" }
    ]
  },
  {
    "value": "{event.reason}",
    "conditions": [{ "field": "event.reason" }]
  }
]
