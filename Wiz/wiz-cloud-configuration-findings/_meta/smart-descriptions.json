[
  {
    "value": "Alert for rule {rule.name} with status {wiz.findings.status}",
    "conditions": [
      {
        "field": "rule.name"
      },
      {
        "field": "wiz.findings.status"
      }
    ]
  },
  {
    "value": "Alert for rule id {rule.id} with status {wiz.findings.status}",
    "conditions": [
      {
        "field": "rule.id"
      },
      {
        "field": "wiz.findings.status"
      }
    ]
  }
]
