[
  {
    "value": "{event.action} request from {source.ip} to {url.original}({destination.ip})",
    "conditions": [
      { "field": "source.ip" },
      { "field": "destination.ip" },
      { "field": "url.original" }
    ]
  },
  {
    "value": "{event.action} request from {source.ip} to {url.original}",
    "conditions": [{ "field": "source.ip" }, { "field": "url.original" }]
  }
]
