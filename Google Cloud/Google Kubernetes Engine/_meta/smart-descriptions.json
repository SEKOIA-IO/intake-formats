[
  {
    "value": "Action type {google_cloud_audit.resource.labels.method} was performed on account {user.email} for service {google_cloud_audit.resource.labels.service}",
    "conditions": [
      {
        "field": "user.email"
      },
      {
        "field": "google_cloud_audit.resource.labels.service"
      },
      {
        "field": "google_cloud_audit.resource.labels.method"
      }
    ]
  },
  {
    "value": "Severity type {google_cloud_audit.severity} for action {google_cloud_audit.protoPayload.request.type} performed by {user.email}",
    "conditions": [
      {
        "field": "user.email"
      },
      {
        "field": "google_cloud_audit.severity"
      },
      {
        "field": "google_cloud_audit.protoPayload.request.type"
      }
    ]
  },
  {
    "value": "{orchestrator.cluster.name} performed action {event.reason} ",
    "conditions": [
      {
        "field": "event.reason"
      },
      {
        "field": "orchestrator.cluster.name"
      }
    ]
  },
  {
    "value": "{event.reason}",
    "conditions": [
      {
        "field": "event.reason"
      }
    ]
  }
]
