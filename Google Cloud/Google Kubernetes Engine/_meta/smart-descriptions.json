[
  {
    "value": "{orchestrator.cluster.name} performed action {event.reason} ",
    "conditions": [
      {
        "field": "event.reason"
      },
      {
        "field": "orchestrator.cluster.name"
      }
    ]
  }
]