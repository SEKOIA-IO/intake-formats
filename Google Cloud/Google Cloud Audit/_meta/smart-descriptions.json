[
  {
    "value": "Service account key {google_cloud_audit.service_account.key.id} was created for {google_cloud_audit.service_account.email} and is valid until epoch {google_cloud_audit.service_account.key.valid_before_time}",
    "conditions": [
      {
        "field": "google_cloud_audit.service_account.key.id"
      },
      {
        "field": "google_cloud_audit.service_account.email"
      },
      {
        "field": "google_cloud_audit.service_account.key.valid_before_time"
      }
    ]
  },
  {
    "value": "Service account key {google_cloud_audit.service_account.key.id} authenticated {user.name} to run {google_cloud_audit.protoPayload.methodName} on {service.name}",
    "conditions": [
      {
        "field": "google_cloud_audit.protoPayload.authenticationInfo.serviceAccountKeyName"
      },
      {
        "field": "google_cloud_audit.service_account.key.id"
      },
      {
        "field": "user.name"
      },
      {
        "field": "google_cloud_audit.protoPayload.methodName"
      },
      {
        "field": "service.name"
      }
    ]
  },
  {
    "value": "Action type {google_cloud_audit.resource.labels.method} was performed on account {user.name} for service {google_cloud_audit.resource.labels.service}",
    "conditions": [
      {
        "field": "user.name"
      },
      {
        "field": "google_cloud_audit.resource.labels.service"
      },
      {
        "field": "google_cloud_audit.resource.labels.method"
      }
    ]
  },
  {
    "value": "Severity type {google_cloud_audit.severity} for action {google_cloud_audit.protoPayload.request.type} performed by {user.name}",
    "conditions": [
      {
        "field": "user.name"
      },
      {
        "field": "google_cloud_audit.severity"
      },
      {
        "field": "google_cloud_audit.protoPayload.request.type"
      }
    ]
  },
  {
    "value": "{google_cloud_audit.jsonPayload.kind} of type {google_cloud_audit.severity} on {host.name} reason {event.reason}",
    "conditions": [
      {
        "field": "google_cloud_audit.jsonPayload.kind"
      },
      {
        "field": "google_cloud_audit.severity"
      },
      {
        "field": "host.name"
      },
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": "{user.name} performed action type {google_cloud_audit.protoPayload.methodName} on {service.name}",
    "conditions": [
      {
        "field": "user.name"
      },
      {
        "field": "google_cloud_audit.protoPayload.methodName"
      },
      {
        "field": "service.name"
      }
    ]
  },
  {
    "value": "Action type {google_cloud_audit.protoPayload.methodName} performed on {google_cloud_audit.protoPayload.resourceName}",
    "conditions": [
      {
        "field": "google_cloud_audit.protoPayload.methodName"
      },
      {
        "field": "google_cloud_audit.protoPayload.resourceName"
      }
    ]
  },
  {
    "value": "Service account key {google_cloud_audit.service_account.key.id} for {google_cloud_audit.service_account.email} was used in {google_cloud_audit.protoPayload.methodName}",
    "conditions": [
      {
        "field": "google_cloud_audit.service_account.key.id"
      },
      {
        "field": "google_cloud_audit.service_account.email"
      },
      {
        "field": "google_cloud_audit.protoPayload.methodName"
      }
    ]
  },
  {
    "value": "{event.reason}",
    "conditions": [
      {
        "field": "event.reason"
      }
    ]
  }
]
