[
  {
    "value": "{action.type} {action.name} from {user.name} on {source.ip}",
    "conditions": [
      { "field": "action.type" },
      { "field": "action.name" },
      { "field": "user.name" },
      { "field": "source.ip" }
    ]
  }
]
