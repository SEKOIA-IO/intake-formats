[
  {
    "value": "Hodor agent {hodor.agent.name} called {event.action} on {service.name} for {user.name}: {event.outcome}",
    "conditions": [
      {
        "field": "hodor.agent.name"
      },
      {
        "field": "event.action"
      },
      {
        "field": "service.name"
      },
      {
        "field": "user.name"
      },
      {
        "field": "event.outcome"
      }
    ]
  },
  {
    "value": "Hodor agent {hodor.agent.id} called {event.action}: {event.outcome}",
    "conditions": [
      {
        "field": "hodor.agent.id"
      },
      {
        "field": "event.action"
      },
      {
        "field": "event.outcome"
      }
    ]
  },
  {
    "value": "Hodor call {event.action}: {event.outcome}",
    "conditions": [
      {
        "field": "event.action"
      },
      {
        "field": "event.outcome"
      }
    ]
  },
  {
    "value": "Hodor activity event",
    "conditions": []
  }
]
