keycloak.realm.id:
  description: The unique identifier of the Keycloak realm
  name: keycloak.realm.id
  type: keyword

keycloak.realm.name:
  description: The name of the Keycloak realm
  name: keycloak.realm.name
  type: keyword

keycloak.service.environment:
  description: The deployment environment of the Keycloak service (e.g., prod, dev,
    staging)
  name: keycloak.service.environment
  type: keyword
