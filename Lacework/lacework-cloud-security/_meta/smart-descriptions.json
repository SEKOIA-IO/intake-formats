[
  {
    "value": "{lacework.cloud_security.alert.severity} severity {event.type} alert issued by {user.name} on Lacework account {lacework.cloud_security.account.name}: {event.reason}",
    "conditions": [
      {
        "field": "lacework.cloud_security.alert.severity"
      },
      {
        "field": "event.type"
      },
      {
        "field": "user.name"
      },
      {
        "field": "lacework.cloud_security.account.name"
      },
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": "{lacework.cloud_security.alert.severity} severity {event.type} alert issued on Lacework account {lacework.cloud_security.account.name}: {event.reason}",
    "conditions": [
      {
        "field": "lacework.cloud_security.alert.severity"
      },
      {
        "field": "event.type"
      },
      {
        "field": "lacework.cloud_security.account.name"
      },
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": "{lacework.cloud_security.alert.severity} severity {event.type} alert issued: {event.reason}",
    "conditions": [
      {
        "field": "lacework.cloud_security.alert.severity"
      },
      {
        "field": "event.type"
      },
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": "Alert: {event.reason}",
    "conditions": [
      {
        "field": "event.reason"
      }
    ]
  }
]
