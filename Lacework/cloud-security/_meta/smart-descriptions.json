[
  {
    "value": "{lacework.alert.severity} severity {event.type} alert issued by {user.name} on Lacework account {lacework.account.name}: {event.reason}",
    "conditions": [
      {
        "field": "lacework.alert.severity"
      },
      {
        "field": "event.type"
      },
      {
        "field": "user.name"
      },
      {
        "field": "lacework.account.name"
      },
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": "{lacework.alert.severity} severity {event.type} alert issued on Lacework account {lacework.account.name}: {event.reason}",
    "conditions": [
      {
        "field": "lacework.alert.severity"
      },
      {
        "field": "event.type"
      },
      {
        "field": "lacework.account.name"
      },
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": "{lacework.alert.severity} severity {event.type} alert issued: {event.reason}",
    "conditions": [
      {
        "field": "lacework.alert.severity"
      },
      {
        "field": "event.type"
      },
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": "Alert: {event.reason}",
    "conditions": [
      {
        "field": "event.reason"
      }
    ]
  }
]
