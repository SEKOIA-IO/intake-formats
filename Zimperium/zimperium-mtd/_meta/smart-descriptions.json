[
  {
    "value": "{event.action} for user {user.email} from {source.ip}",
    "conditions": [
      { "field": "event.action" },
      { "field": "user.email" },
      { "field": "source.ip" }
    ]
  },
  {
    "value": "{event.action} for user {user.name} from {source.ip}",
    "conditions": [
      { "field": "event.action" },
      { "field": "user.name" },
      { "field": "source.ip" }
    ]
  }
]
