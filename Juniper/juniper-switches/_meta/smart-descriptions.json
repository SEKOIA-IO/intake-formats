[
  {
    "value": "{network.transport} connection on interface {observer.ingress.interface.name} from {source.ip}:{source.port}({source.mac}) to {destination.ip}:{destination.port}({destination.mac})",
    "conditions": [
      { "field": "source.ip" },
      { "field": "destination.ip" },
      { "field": "source.port" },
      { "field": "destination.port" },
      { "field": "source.mac" },
      { "field": "destination.mac" }
    ]
  },
  {
    "value": "{network.transport} connection on interface {observer.ingress.interface.name} from {source.ip}:{source.port} to {destination.ip}:{destination.port}",
    "conditions": [
      {
        "field": "source.ip"
      },
      {
        "field": "destination.ip"
      },
      {
        "field": "source.port"
      },
      {
        "field": "destination.port"
      }
    ]
  }
]
