[
    {
        "value": "Email with Message-ID {{email.message_id}} was QUEUED",
        "conditions": [
            {
                "field": "{{cisco.esa.status}}",
                "value": "QUEUED"
            },
            {
                "field": "{{email.message_id}}"
            }
        ],
    "relationships": []
  },{
        "value": "Email with Message-ID {{email.message_id}} was delivered",
        "conditions": [
            {
                "field": "{{cisco.esa.status}}"
            },
            {
                "field": "{{email.message_id}}"
            }
        ],
    "relationships": []
  }
]



