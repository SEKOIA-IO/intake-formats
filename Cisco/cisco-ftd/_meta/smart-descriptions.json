{
    "value": "Connection from {source.ip} to {destination.ip}:{destination.port} was {action.name}",
    "conditions": [
        {
            "field": "source.ip",
            "field": "destination.ip",
            "field": "action.name"
        }
    ],
    "relationships": [
        {
            "source": "source.ip",
            "target": "destination.ip",
            "type": "requested"
        }
    ]
}