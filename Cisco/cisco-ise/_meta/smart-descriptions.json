[
  {
    "value": "Configuration have been {cisco.ise.config_action} for the Device {cisco.ise.config_object.name}",
    "conditions": [
      {
        "field": "cisco.ise.config_action"
      },
      {
        "field": "cisco.ise.config_object.name"
      }
    ]
  },
  {
    "value": " Connection from {source.ip} to the server {source.domain} and this is the reason {event.reason}",
    "conditions": [
      {
        "field": "source.ip"
      },
      {
        "field": "source.domain"
      },
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": " Connection from {source.ip} to the server {source.domain}",
    "conditions": [
      {
        "field": "source.ip"
      },
      {
        "field": "source.domain"
      }
    ]
  },
  {
    "value": " Reason : {event.reason}",
    "conditions": [
      {
        "field": "event.reason"
      }
    ]
  }
]
