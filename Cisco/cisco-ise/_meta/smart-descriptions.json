[
  {
    "value": "Configuration have been {cisco.ise.config_action} for the Device {cisco.ise.config_object.name}",
    "conditions": [
      {
        "field": "cisco.ise.config_action"
      },
      {
        "field": "cisco.ise.config_object.name"
      }
    ]
  },
  {
    "value": " Connection from {source.ip} to the server {source.domain} and this is the reason {event.reason}",
    "conditions": [
      {
        "field": "source.ip"
      },
      {
        "field": "source.domain"
      },
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": " Connection from {source.ip} to the server {source.domain}",
    "conditions": [
      {
        "field": "source.ip"
      },
      {
        "field": "source.domain"
      }
    ]
  },
  {
    "value": " Reason : {event.reason}",
    "conditions": [
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": "An event {cisco.ise.type} has been detected",
    "conditions": [
      {
        "field": "cisco.ise.type"
      }
    ]
  },
  {
    "value": "Subject Common Name {tls.client.x509.subject.common_name}, Subject Alternative Names {tls.client.x509.subject.alternative_names}",
    "conditions": [
      {
        "field": "tls.client.x509.subject.common_name"
      },
      {
        "field": "tls.client.x509.subject.alternative_names"
      }
    ]
  }
]
