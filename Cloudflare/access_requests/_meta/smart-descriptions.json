[
  {
    "value": "{user.id} ({source.ip}) accessed {url.domain} from {source.geo.country_iso_code} result: {event.type}",
    "conditions": [
      {
        "field": "user.id"
      },
      {
        "field": "source.ip"
      },
      {
        "field": "url.domain"
      },
      {
        "field": "source.geo.country_iso_code"
      },
      {
        "field": "event.type"
      }
    ]
  },
  {
    "value": "{user.email} ({source.ip}) performed {event.outcome} action on {event.action}",
    "conditions": [
        {
            "field": "user.email"
        },
        {
            "field": "source.ip"
        },
        {
            "field": "event.outcome"
        },
        {
            "field": "event.action"
        }
    ]
  }
]
