[
  {
    "value": "{host.name} ( {user.email} ) requested the resolution of {dns.question.name} from {source.ip}",
    "conditions": [
      {
        "field": "source.ip"
      },
      {
        "field": "host.name"
      },
      {
        "field": "user.email"
      },
      {
        "field": "dns.question.name"
      }
    ]
  },
  {
    "value": "{source.ip} requested the resolution of {dns.question.name}",
    "conditions": [
      {
        "field": "source.ip"
      },
      {
        "field": "dns.question.name"
      }
    ]
  }
]
