[
  {
    "value": "The user {user.name} logged in from {network.name} with ip {client.ip} and port {client.port}",
    "conditions": [
      {
        "field": "user.name"
      },
      {
        "field": "client.ip"
      },
      {
          "field": "client.port"
      },
      {
          "field": "network.name"
      },
      {
        "field": "freeradius.outcome",
        "value": "Login OK"
      }
    ]
  },
  {
    "value": "The user {user.name} logged in from {network.name} with mac {client.mac} and port {client.port}",
    "conditions": [
      {
        "field": "user.name"
      },
      {
        "field": "client.mac"
      },
      {
          "field": "client.port"
      },
      {
          "field": "network.name"
      },
      {
        "field": "freeradius.outcome",
        "value": "Login OK"
      }
    ]
  },
  {
    "value": "The host {client.domain} logged in from {network.name} with mac {client.mac} and port {client.port}",
    "conditions": [
      {
        "field": "client.domain"
      },
      {
        "field": "client.mac"
      },
      {
          "field": "client.port"
      },
      {
          "field": "network.name"
      },
      {
        "field": "freeradius.outcome",
        "value": "Login OK"
      }
    ]
  },
  {
    "value": "The user {user.name} successfully logged in",
    "conditions": [
      {
        "field": "user.name"
      },
      {
        "field": "freeradius.outcome",
        "value": "Login OK"
      }
    ]
  },
  {
    "value": "The user {user.name} tried to log in with status {freeradius.outcome}: {event.reason}",
    "conditions": [
      {
	"field": "user.name"
      },
      {
	"field": "event.reason"
      }
    ]
  },
  {
    "value": "The host {client.domain} successfully logged in",
    "conditions": [
      {
        "field": "client.domain"
      },
      {
        "field": "freeradius.outcome",
        "value": "Login OK"
      }
    ]
  },
  {
    "value": "The host {client.domain} tried to log in with status {freeradius.outcome}: {event.reason}",
    "conditions": [
      {
	"field": "client.domain"
      },
      {
	"field": "event.reason"
      }
    ]
  }
]
