[
  {
    "value": "{event.reason} from {source.ip}:{source.port} to {destination.ip}:{destination.port}",
    "conditions": [
      { "field": "event.reason" },
      { "field": "source.ip" },
      { "field": "source.port" },
      { "field": "destination.ip" },
      { "field": "destination.port" }
    ]
  },
  {
    "value": "{event.reason} from {source.ip} to {destination.ip}",
    "conditions": [
      { "field": "event.reason" },
      { "field": "source.ip" },
      { "field": "destination.ip" }
    ]
  },
  {
    "value": "{event.reason}",
    "conditions": [{ "field": "event.reason" }]
  }
]
