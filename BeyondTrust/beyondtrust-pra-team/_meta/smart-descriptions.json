[
  {
    "value": "{event.code} by {beyondtrust.pra.performed_by.type} {user.name}",
    "conditions": [
      {
        "field": "event.code"
      },
      {
        "field": "beyondtrust.pra.performed_by.type"
      },
      {
        "field": "user.name"
      }
    ]
  },
  {
    "value": "{event.code} on team {group.name} from NAT {source.nat.ip}:{source.nat.port}",
    "conditions": [
      {
        "field": "event.code"
      },
      {
        "field": "group.name"
      },
      {
        "field": "source.nat.ip"
      },
      {
        "field": "source.nat.port"
      }
    ]
  }
]
