[
  {
    "value": "{event.code} for {user.target.id} by {beyondtrust.pra.performed_by.type} {user.name}({user.id})",
    "conditions": [
      {
        "field": "event.code"
      },
      {
        "field": "user.target.id"
      },
      {
        "field": "beyondtrust.pra.performed_by.type"
      },
      {
        "field": "user.name"
      },
      {
        "field": "user.id"
      }
    ]
  }
]
