[
  {
    "value": "{event.category} {event.type}: {event.action} by {user.full_name} from {source.ip} on {host.name}",
    "conditions": [
      { "field": "event.category" },
      { "field": "event.type" },
      { "field": "event.action" },
      { "field": "user.full_name" },
      { "field": "source.ip" },
      { "field": "host.name" }
    ]
  },
  {
      "value": "{event.category} {event.type}:{event.action} from {source.ip} on {host.name}",
    "conditions": [
      { "field": "event.category" },
      { "field": "event.type" },
      { "field": "event.action" },
      { "field": "source.ip" },
      { "field": "host.name" }
    ]
  },
  {
    "value": "{event.action} by {user.full_name} from {source.ip} on {host.name}",
    "conditions": [
      { "field": "event.action" },
      { "field": "user.full_name" },
      { "field": "source.ip" },
      { "field": "host.name" }
    ]
  },
  {
    "value": "{event.action} by {user.full_name} from {source.ip}",
    "conditions": [
      { "field": "event.action" },
      { "field": "user.full_name" },
      { "field": "source.ip" }
    ]
  },
  {
    "value": "{event.action} from {source.ip} on {host.name}",
    "conditions": [
      { "field": "event.action" },
      { "field": "source.ip" },
      { "field": "host.name" }
    ]
  },
  {
    "value": "{event.action} on {host.name}",
    "conditions": [{ "field": "event.action" }, { "field": "host.name" }]
  }
]
