[
  {
    "value": "{event.dataset} event from user {user.name}({source.ip}) with status {action.outcome}: {event.reason}",
    "conditions": [
      { "field": "action.outcome" },
      { "field": "event.dataset" },
      { "field": "event.reason" },
      { "field": "user.name" },
      { "field": "source.ip" }
    ]
  },
  {
    "value": "{event.dataset} event from user {user.name}({source.ip}): {event.reason}",
    "conditions": [
      { "field": "event.dataset" },
      { "field": "event.reason" },
      { "field": "user.name" },
      { "field": "source.ip" }
    ]
  },
  {
    "value": "{event.dataset} event from user {user.name}  with status {action.outcome}: {event.reason}",
    "conditions": [
      { "field": "action.outcome" },
      { "field": "event.dataset" },
      { "field": "event.reason" },
      { "field": "user.name" }
    ]
  },
  {
    "value": "{event.dataset} event from user {user.name}: {event.reason}",
    "conditions": [
      { "field": "event.dataset" },
      { "field": "event.reason" },
      { "field": "user.name" }
    ]
  },
  {
    "value": "{event.dataset} event from {source.ip} with status {action.outcome}: {event.reason}",
    "conditions": [
      { "field": "action.outcome" },
      { "field": "event.dataset" },
      { "field": "event.reason" },
      { "field": "source.ip" }
    ]
  },
  {
    "value": "{event.dataset} event from {source.ip}: {event.reason}",
    "conditions": [
      { "field": "event.dataset" },
      { "field": "event.reason" },
      { "field": "source.ip" }
    ]
  },
  {
    "value": "{event.dataset} event on file {file.name} at {file.path}: {event.reason}",
    "conditions": [
      { "field": "event.dataset" },
      { "field": "event.reason" },
      { "field": "file.name" },
      { "field": "file.path" }
    ]
  },
  {
    "value": "Alert: {event.reason}",
    "conditions": [
      { "field": "event.kind", "value": "alert" },
      { "field": "event.reason" }
    ]
  },
  {
    "value": "{event.dataset} event: {event.reason}",
    "conditions": [{ "field": "event.dataset" }, { "field": "event.reason" }]
  }
]
