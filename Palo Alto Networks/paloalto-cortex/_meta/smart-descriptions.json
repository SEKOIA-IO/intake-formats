[
  {
    "value": "{event.category}: An event of severity {event.severity} was {event.action} on endpoint {host.name}, initiated by user {user.domain}\\{user.name}.",
    "conditions": [
      {
        "field": "event.category"
      },
      {
        "field": "event.severity"
      },
      {
        "field": "event.action"
      },
      {
        "field": "host.name"
      },
      {
        "field": "user.domain"
      },
      {
        "field": "user.name"
      }
    ]
  },
  {
    "value": "{event.category}: {event.reason}",
    "conditions": [
      {
        "field": "event.category"
      },
      {
        "field": "event.reason"
      }
    ]
  }
]
