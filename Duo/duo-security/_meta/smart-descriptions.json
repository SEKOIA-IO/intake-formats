[
  {
    "value": "Operation {event.action} performed on {duo.security.object} by {user.name}",
    "conditions": [
      {
        "field": "event.dataset",
        "value": "offline_log"
      },
      {
        "field": "event.action"
      },
      {
        "field": "duo.security.object"
      },
      {
        "field": "user.name"
      }
    ]
  },
  {
    "value": "Operation {event.action} performed by {user.name}",
    "conditions": [
      {
        "field": "event.dataset",
        "value": "offline_log"
      },
      {
        "field": "event.action"
      },
      {
        "field": "user.name"
      }
    ]
  },
  {
    "value": "{user.email} authorized from {source.ip}",
    "conditions": [
      {
        "field": "event.dataset",
        "value": "auth_log"
      },
      {
        "field": "user.email"
      },
      {
        "field": "source.ip"
      }
    ]
  },
  {
    "value": "Operation {event.action} performed on {duo.security.object} by {user.name}",
    "conditions": [
      {
        "field": "event.dataset",
        "value": "admin_log"
      },
      {
        "field": "event.action"
      },
      {
        "field": "duo.security.object"
      },
      {
        "field": "user.name"
      }
    ]
  },
  {
    "value": "Operation {event.action} performed by {user.name}",
    "conditions": [
      {
        "field": "event.dataset",
        "value": "admin_log"
      },
      {
        "field": "event.action"
      },
      {
        "field": "user.name"
      }
    ]
  },
  {
    "value": "Admin login error: {event.reason} for {user.email} on {source.ip}",
    "conditions": [
      {
        "field": "event.action",
        "value": "admin_login_error"
      },
      {
        "field": "event.reason"
      },
      {
        "field": "user.email"
      },
      {
        "field": "source.ip"
      }
    ]
  },
  {
    "value": "{duo.security.telephony.phone_number} was used for {event.reason}",
    "conditions": [
      {
        "field": "event.dataset",
        "value": "telephony_log"
      },
      {
        "field": "duo.security.telephony.phone_number"
      },
      {
        "field": "event.reason"
      }
    ]
  }
]
