[
  {
    "value": "User {user.name} executed {process.executable} (pid {process.pid}, ppid {process.parent.pid}) on {host.hostname}",
    "conditions": [
      {
        "field": "event.category",
        "value": "process"
      },
      {
        "field": "event.type",
        "value": "start"
      }
    ]
  },
  {
    "value": "{process.executable} (pid {process.pid}) on {host.hostname} connected {source.ip}:{source.port} → {destination.ip}:{destination.port} ({network.transport}), user {user.name}",
    "conditions": [
      {
        "field": "event.category",
        "value": "network"
      },
      {
        "field": "event.type",
        "value": "connection"
      }
    ]
  },
  {
    "value": "User {user.name} with process {process.executable} (pid {process.pid}) read {file.path} on {host.hostname}",
    "conditions": [
      {
        "field": "event.category",
        "value": "file"
      },
      {
        "field": "event.type",
        "value": "access"
      }
    ]
  },
  {
    "value": "User {user.name} with process {process.executable} (pid {process.pid}) modified {file.path} on {host.hostname}",
    "conditions": [
      {
        "field": "event.category",
        "value": "file"
      },
      {
        "field": "event.type",
        "value": "change"
      }
    ]
  },
  {
    "value": "User {user.name} with process {process.executable} (pid {process.pid}) deleted {file.path} on {host.hostname}",
    "conditions": [
      {
        "field": "event.category",
        "value": "file"
      },
      {
        "field": "event.type",
        "value": "deletion"
      }
    ]
  },
  {
    "value": "{process.executable} (pid {process.pid}) opened a process {file.path} on {host.hostname}, user {user.name}",
    "conditions": [
      {
        "field": "event.category",
        "value": "process"
      },
      {
        "field": "event.type",
        "value": "info"
      }
    ]
  }
]