[
  {
    "value": "Radware DefensePro detected attack '{Radware.attack.name}' from {source.address}:{source.port} targeting {destination.address}:{destination.port} with risk '{DefensePro.risk}'",
    "conditions": [
      { "field": "Radware.attack.name" },
      { "field": "source.address" },
      { "field": "destination.address" }
    ],
    "relationships": [
      {
        "source": "source.address",
        "target": "destination.address",
        "type": "attempted to connect to"
      }
    ]
  },
  {
    "value": "Drop action triggered for attack ID {Radware.id} on Radware's port {DefensePro.physical.port} using policy '{DefensePro.policy.name}'",
    "conditions": [
      { "field": "action.name", "value": "drop" },
      { "field": "Radware.id" }
    ]
  },
  {
    "value": "ERT category '{DefensePro.category}' detected from {source.address} with severity {log.syslog.severity.name} and context '{Radware.context}'",
    "conditions": [
      { "field": "DefensePro.category", "value": "ErtFeed" }
    ]
  }
]
