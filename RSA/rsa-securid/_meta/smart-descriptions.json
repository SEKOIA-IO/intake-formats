[
  {
    "value": "The user {user.name} logged in from {source.ip} to {destination.ip}",
    "conditions": [
      {
        "field": "user.name"
      },
      {
        "field": "source.ip"
      },
      {
        "field": "destination.ip"
      }
    ]
  },
    {
    "value": "The user {user.name} successfully logged in",
    "conditions": [
      {
        "field": "user.name"
      },
      {
        "field": "log.level",
        "value": "INFO"
      }
    ]
  },  {
    "value": "The user {user.name} tried to log in with status {log.level}: {event.reason}",
    "conditions": [
      {
        "field": "user.name"
      },
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": "The host {observer.hostname} tried to log in with status {log.level}, event outcome: {event.outcome}",
    "conditions": [
      {
        "field": "observer.hostname"
      },
      {
        "field": "log.level"
      },
      {
        "field": "event.outcome"
      }
    ]
  },{
    "value": "{event.reason}",
    "conditions": [
      {
        "field": "event.reason"
      }
    ]
  }
]