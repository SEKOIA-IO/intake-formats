[
  {
    "value": "{event.action} an email tagged as {vadecloud.tag} from {email.from.address} to {email.to.address} with subject {email.subject}",
    "conditions": [
      { "field": "event.action" },
      { "field": "vadecloud.tag" },
      { "field": "email.from.address" },
      { "field": "email.to.address" },
      { "field": "email.subject" }
    ]
  }
]
