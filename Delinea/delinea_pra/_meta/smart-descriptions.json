[
  {
    "value": "Action {event.action} performed on {destination.address} by {source.user.name} from {source.address}",
    "conditions": [
      { "field": "event.action" },
      { "field": "destination.address" },
      { "field": "source.user.name" },
      { "field": "source.address" }
    ]
  },
  {
    "value": "Action {event.action} performed by {source.user.name}: {event.reason}",
    "conditions": [
      { "field": "event.action" },
      { "field": "source.user.name" },
      { "field": "event.reason" }
    ]
  },
  {
    "value": "Event by {source.user.name} on {host.id}: {event.reason}",
    "conditions": [
      { "field": "source.user.name" },
      { "field": "host.id" },
      { "field": "event.reason" }
    ]
  },
  {
    "value": "Event by {source.user.name} for {url.original}: {event.reason}",
    "conditions": [
      { "field": "source.user.name" },
      { "field": "url.original" },
      { "field": "event.reason" }
    ]
  },
  {
    "value": "Event by {source.user.name} with error {error.message}: {event.reason}",
    "conditions": [
      { "field": "source.user.name" },
      { "field": "error.message" },
      { "field": "event.reason" }
    ]
  },
  {
    "value": "Event by {source.user.name}: {event.reason}",
    "conditions": [{ "field": "source.user.name" }, { "field": "event.reason" }]
  },
  {
    "value": "Registration {delinea.pra.registration.id} event: {event.reason}",
    "conditions": [
      { "field": "delinea.pra.registration.id" },
      { "field": "event.reason" }
    ]
  },
  {
    "value": "Event: {event.reason}",
    "conditions": [{ "field": "event.reason" }]
  }
]
