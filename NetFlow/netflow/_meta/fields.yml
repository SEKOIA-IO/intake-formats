netflow.bgp.next_hop:
  description: Address of next-hop router in the BGP domain
  name: netflow.bgp.next_hop
  type: keyword

netflow.next_hop:
  description: Address of next-hop router
  name: netflow.next_hop
  type: keyword

netflow.tcp.flags:
  description: Cumulative of all the TCP flags seen for this flow
  name: netflow.tcp.flags
  type: number
