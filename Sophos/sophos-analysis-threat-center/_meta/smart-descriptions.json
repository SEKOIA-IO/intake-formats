[
  {
    "value": "{vulnerability.description} on {host.name} : {event.reason}",
    "conditions": [
      {
        "field": "vulnerability.description"
      },
      {
        "field": "host.name"
      },
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": "{vulnerability.description} on {host.name}",
    "conditions": [
      {
        "field": "vulnerability.description"
      },
      {
        "field": "host.name"
      }
    ]
  },
  {
    "value": " This query is {sophos.threat_center.query.name}",
    "conditions": [
      {
        "field": "sophos.threat_center.query.name"
      }
    ]
  }
]
