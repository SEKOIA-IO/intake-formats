[
  {
    "value": "successfully query the host {host.name} with the ip {source.ip} ",
    "conditions": [
      {
        "field": "host.name"
      },
      {
        "field": "source.ip"
      }
    ]
  },

  {
    "value": " The query was from {sophos.xdr.query.source}",
    "conditions": [
      {
        "field": "sophos.xdr.query.source"
      }
    ]
  }
]
