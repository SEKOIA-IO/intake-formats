[
  {
    "value": "{user.name} connect to {destination.registered_domain} with Port {source.port} and {network.transport}",
    "conditions": [
      {
        "field": "user.name"
      }
    ],
    "relationships": [
      {
        "source": "user.name",
        "target": "destination.registered_domain",
        "type": "connect to"
      }
    ]
  },
  {
    "value": "{destination.ip} {sophos.action} {sophos.sub} from {source.ip} Port {source.port}",
    "conditions": [
      {
        "field": "sophos.action",
        "value": "accept"
      }
    ],
    "relationships": [
      {
        "source": "source.ip",
        "target": "destination.ip",
        "type": "accept"
      }
    ]
  },
  {
    "value": "{source.ip} make a {sophos.action} to {destination.ip} with Port {source.port}",
    "conditions": [
      {
        "field": "sophos.action",
        "value": "DNS request"
      }
    ],
    "relationships": [
      {
        "source": "source.ip",
        "target": "destination.ip",
        "type": "make a"
      }
    ]
  },
  {
    "value": "{destination.ip} {sophos.action} {sophos.sub} from {source.ip} Port {source.port}",
    "conditions": [
      {
        "field": "sophos.action",
        "value": "drop"
      }
    ],
    "relationships": [
      {
        "source": "source.ip",
        "target": "destination.ip",
        "type": "drop"
      }
    ]
  },
  {
    "value": "{destination.ip} allow {sophos.action} {network.transport} connection from {source.ip} Port {source.port}",
    "conditions": [
      {
        "field": "network.transport",
        "value": "UDP"
      }
    ],
    "relationships": [
      {
        "source": "source.ip",
        "target": "destination.ip",
        "type": "allow"
      }
    ]
  }
]
