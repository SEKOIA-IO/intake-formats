[
  {
    "value": "{event.dataset} event for user '{user.name}' from {source.ip}: {event.reason}",
    "conditions": [
      { "field": "source.ip" },
      { "field": "user.name" },
      { "field": "event.reason" },
      { "field": "event.dataset" }
    ]
  },
  {
    "value": "{event.dataset} event from {source.ip}: {event.reason}",
    "conditions": [
      { "field": "source.ip" },
      { "field": "event.reason" },
      { "field": "event.dataset" }
    ]
  },
  {
    "value": "{event.category} event for user '{user.name}' from {source.ip}: {event.reason}",
    "conditions": [
      { "field": "source.ip" },
      { "field": "user.name" },
      { "field": "event.reason" },
      { "field": "event.category" }
    ]
  },
  {
    "value": "{event.category} event from {source.ip}: {event.reason}",
    "conditions": [
      { "field": "source.ip" },
      { "field": "event.reason" },
      { "field": "event.category" }
    ]
  },
  {
    "value": "{event.reason}",
    "conditions": [{ "field": "event.reason" }]
  }
]
