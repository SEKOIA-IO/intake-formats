[
  {
    "value": "The user {user.name} cannot login with the ip address: {vmware_vcenter.ip_address}",
    "conditions": [
      {
        "field": "vmware_vcenter.ip_address"
      },
      {
        "field": "user.name"
      }
    ],
    "relationships": [
      {
        "target": "user.name",
        "type": "cannot login"
      }
    ]
  },  {
    "value": "The ip address {vmware_vcenter.ip_address} logged in as {user.name}",
    "conditions": [
      {
        "field": "vmware_vcenter.ip_address"
      },
      {
        "field": "user.name"
      }
    ],"relationships": [
      {
        "target": "user.name",
        "type": "logged in as"
      }
    ]
  },  {
    "value": "The {user_agent.original} login time is {vmware_vcenter.login_time} with the ip address : {vmware_vcenter.ip_address} ",
    "conditions": [
      {
        "field": "vmware_vcenter.ip_address"
      },
      {
        "field": "user_agent.original"
      },
      {
        "field": "vmware_vcenter.login_time"
      }
    ]
  },
  {
    "value": "Session was opened for {user.name} with the ip address: {vmware_vcenter.ip_address}",
    "conditions": [
      {
        "field": "vmware_vcenter.ip_address"
      },
      {
        "field": "user.name"
      }
    ],
    "relationships": [
      {
        "target": "user.name",
        "type": "was opened for"
      }
    ]
  },
  {
    "value": "Session was closed for {user.name} with the ip address: {vmware_vcenter.ip_address}",
    "conditions": [
      {
        "field": "vmware_vcenter.ip_address"
      },
      {
        "field": "user.name"
      }
    ],
    "relationships": [
      {
        "target": "user.name",
        "type": "was closed for"
      }
    ]
  },
  {
    "value": "the event reason is : {event.reason} and the event code : {event.code}",
    "conditions": [
      {
        "field": "event.reason"
      },
      {
        "field": "event.code"
      }
    ]
  },
    {
    "value": "the event code : {event.code} and the user is : {user.name}",
    "conditions": [
      {
        "field": "event.code"
      },
      {
        "field": "user.name"
      }
    ]
  },
  {
    "value": "the event at the datetime {vmware_vcenter.datetime} with the http method {http.request.method} and the url path {url.path}, connected with the source ip {source.ip} and port {client.port} to {destination.ip}:{destination.port}",
    "conditions": [
      {
        "field": "vmware_vcenter.datetime"
      },
      {
        "field": "http.request.method"
      },
      {
        "field": "url.path"
      },
      {
        "field": "source.ip"
      },
      {
        "field": "client.port"
      },
      {
        "field": "destination.ip"
      },
      {
        "field": "destination.port"
      }
    ]
  },
    {
    "value": "the event at the datetime {vmware_vcenter.datetime} with the http method {http.request.method} and the url path {url.path}, connected with the source ip {source.ip} and port {client.port} to {destination.port} with a response status : {http.response.status_code}",
    "conditions": [
      {
        "field": "vmware_vcenter.datetime"
      },
      {
        "field": "http.request.method"
      },
      {
        "field": "url.path"
      },
      {
        "field": "source.ip"
      },
      {
        "field": "client.port"
      },
      {
        "field": "http.response.status_code"
      },
      {
        "field": "destination.port"
      }
    ]
  },
  {
    "value": "the source address is {source.address}",
    "conditions": [
      {
        "field": "source.address"
      }
    ]
  },
  {
    "value": "{event.reason}",
    "conditions": [
      {
        "field": "event.reason"
      }
    ]
  }
]