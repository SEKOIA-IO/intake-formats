[
  {
    "value": "The event is generate by {user.name} with the ip address: {vmware_vcenter.ip_address}",
    "conditions": [
      {
        "field": "vmware_vcenter.ip_address"
      },
      {
        "field": "user.name"
      }
    ]
  },{
    "value": "The event is generate by the user agent {user_agent.original} with the ip address: {vmware_vcenter.ip_address} at the login time {vmware_vcenter.login_time}",
    "conditions": [
      {
        "field": "vmware_vcenter.ip_address"
      },
      {
        "field": "user_agent.original"
      },
      {
        "field": "vmware_vcenter.login_time"
      }
    ]
  },
   {
    "value": " the event reason is : {event.reason}",
    "conditions": [
      {
        "field": "event.reason"
      }
    ]
  }

]