[
  {
    "value": "The event is generate by {user.name} with the ip address: {vmware_vcenter.ip_address}",
    "conditions": [
      {
        "field": "vmware_vcenter.ip_address"
      },
      {
        "field": "user.name"
      }
    ]
  },{
    "value": "The event is generate by the user agent {user_agent.original} with the ip address: {vmware_vcenter.ip_address} at the login time {vmware_vcenter.login_time}",
    "conditions": [
      {
        "field": "vmware_vcenter.ip_address"
      },
      {
        "field": "user_agent.original"
      },
      {
        "field": "vmware_vcenter.login_time"
      }
    ]
  },
   {
    "value": " the event reason is : {event.reason}",
    "conditions": [
      {
        "field": "event.reason"
      }
    ]
  },
  {
    "value": " the event reason is : {event.reason} with the event code {event.code}",
    "conditions": [
      {
        "field": "event.reason"
      },
      {
        "field": "event.code"
      }

    ]
  },
  {
    "value": " the event code is : {event.code} , event generate by the username {user.name}",
    "conditions": [
      {
        "field": "event.code"
      },
      {
        "field": "user.name"
      }

    ]
  },
  {
    "value": " the event source address is : {source.address}",
    "conditions": [
      {
        "field": "source.address"
      }
    ]
  }
]