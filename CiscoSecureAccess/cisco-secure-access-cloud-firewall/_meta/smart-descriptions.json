[
  {
    "value": "{event.action} {network.direction} traffic from {source.ip}:{source.port} to {destination.ip}:{destination.port}",
    "conditions": [
      { "field": "event.action" },
      { "field": "network.direction" },
      { "field": "source.ip" },
      { "field": "source.port" },
      { "field": "destination.ip" },
      { "field": "destination.port" }
    ]
  }
]
