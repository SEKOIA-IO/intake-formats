[
  {
    "value": "{event.action} email with subject {email.subject} from {email.sender.address} to {email.to.address}",
    "conditions": [
      { "field": "event.action" },
      { "field": "event.subject" },
      { "field": "event.sender.address" },
      { "field": "event.to.address" }
    ]
  },
  {
    "value": "{event.action} email from {email.sender.address} to {email.to.address}",
    "conditions": [
      { "field": "event.action" },
      { "field": "event.sender.address" },
      { "field": "event.to.address" }
    ]
  }
]
