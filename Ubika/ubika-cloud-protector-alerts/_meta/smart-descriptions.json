[
  {
    "value": "Blocked {http.request.method} request to {url.path} from {source.ip}: {event.reason}",
    "conditions": [
      { "field": "http.request.method" },
      { "field": "url.path" },
      { "field": "source.ip" },
      { "field": "event.reason" }
    ]
  }
]
