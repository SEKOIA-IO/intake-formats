[
  {
    "value": "Request from {source.ip} ({user_agent.original}) to {destination.address} {url.path} was blocked for reason {rule.name}",
    "conditions": [
      {
        "field": "event.type.0",
        "value": "indicator"
      },
      {
        "field": "rule.name"
      }
    ]
  },
  {
    "value": "{http.request.method} request from {source.ip}: {url.original} (status: {http.response.status_code}, bytes: {http.response.body.bytes})",
    "relationships": [
      {
        "source": "source.ip",
        "target": "url.original",
        "type": "requested"
      }
    ],
    "conditions": [
      {
        "field": "url.original"
      },
      {
        "field": "event.type.0",
        "value": "access"
      }
    ]
  }
]
