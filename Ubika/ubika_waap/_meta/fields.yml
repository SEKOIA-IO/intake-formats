ubika.waap.tunnel.name:
  name: ubika.waap.tunnel.name
  type: keyword
  description: Tunnel name

ubika.waap.tunnel.uuid:
  name: ubika.waap.tunnel.uuid
  type: keyword
  description: Tunnel UID

ubika.waap.workflow.name:
  name: ubika.waap.workflow.name
  type: keyword
  description: Workflow name

ubika.waap.workflow.uuid:
  name: ubika.waap.workflow.uuid
  type: keyword
  description: Workflow UID
