[
  {
    "value": "Operation {action.name} performed on device {host.name}",
    "conditions": [
      {
        "field": "host.name"
      },
      {
        "field": "action.name"
      }
    ],
    "relationships": [
      {
        "source": "host.name",
        "target": "action.name",
        "type": "performed operation"
      }
    ]
  },
  {
    "value": "Operation {action.name} performed by {user.name}",
    "conditions": [
      {
        "field": "user.name"
      },
      {
        "field": "action.name"
      }
    ],
    "relationships": [
      {
        "source": "user.name",
        "target": "action.name",
        "type": "performed operation"
      }
    ]
  },
  {
    "value": "{user.name} logged on device {host.name}",
    "conditions": [
      {
        "field": "host.name"
      },
      {
        "field": "user.name"
      }
    ],
    "relationships": [
      {
        "source": "user.name",
        "target": "host.name",
        "type": "logged on"
      }
    ]
  },
  {
    "value": "{host.name} compliance status evaluated at {microsoft.intune.compliant_state}",
    "conditions": [
      {
        "field": "host.name"
      },
      {
        "field": "microsoft.intune.compliant_state"
      }
    ]
  }
]
