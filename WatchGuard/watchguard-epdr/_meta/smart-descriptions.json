[
  {
    "value": "Exploit {threat.technique.name} detected on {host.name} and file {file.path}",
    "conditions": [
      {
        "field": "threat.technique.name"
      },
      {
        "field": "host.name"
      },
      {
        "field": "file.path"
      }
    ]
  },
  {
    "value": "Malware {watchguard.epdr.malware.name} with type {watchguard.epdr.malware.type} detected",
    "conditions": [
      {
        "field": "watchguard.epdr.malware.name"
      },
      {
        "field": "watchguard.epdr.malware.type"
      }
    ]
  },
  {
    "value": "Event with type {event.type} occurred on {host.name} for {file.path}",
    "conditions": [
      {
        "field": "event.type"
      },
      {
        "field": "host.name"
      },
      {
        "field": "file.path"
      }
    ]
  },
  {
    "value": "Event with category {watchguard.epdr.malware.category} occurred on {host.name} for {file.path}",
    "conditions": [
      {
        "field": "watchguard.epdr.malware.category"
      },
      {
        "field": "host.name"
      },
      {
        "field": "file.path"
      }
    ]
  },
  {
    "value": "{rule.name} occurred on {host.name} for {watchguard.epdr.site.name}",
    "conditions": [
      {
        "field": "rule.name"
      },
      {
        "field": "watchguard.epdr.site.name"
      },
      {
        "field": "host.name"
      }
    ]
  },
  {
    "value": "New event occurred on {host.name} for {watchguard.epdr.site.name}",
    "conditions": [
      {
        "field": "watchguard.epdr.site.name"
      },
      {
        "field": "host.name"
      }
    ]
  },
  {
    "value": "Event with type {event.type} occurred on {host.name}",
    "conditions": [
      {
        "field": "event.type"
      },
      {
        "field": "host.name"
      }
    ]
  },
  {
    "value": "New event occurred on {host.name}",
    "conditions": [
      {
        "field": "host.name"
      }
    ]
  }
]
